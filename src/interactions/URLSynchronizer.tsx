import * as React from 'react';

interface Props {}

const URLSynchronizer: React.FunctionComponent<Props> = props => {
    return null;
};

URLSynchronizer.defaultProps = {};
URLSynchronizer.displayName = 'URLSynchronizer';

export default URLSynchronizer;
